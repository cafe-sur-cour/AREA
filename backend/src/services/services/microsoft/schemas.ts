import type { ActionReactionSchema } from '../../../types/mapping';

export const microsoftProfilePictureChangedSchema: ActionReactionSchema = {
  name: 'Profile Picture Changed',
  description: 'Triggers when your Microsoft profile picture is changed',
  fields: [],
};
